import 'package:flutter/material.dart';

class GeneresPage extends StatelessWidget {
  const GeneresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Generes")),
    );
  }
}
